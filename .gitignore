node_modules
dist
.env
.env.local
.env.*.local

# Local-only directories (backups, APKs, credentials, scripts)
OTROS/
APK/
.claude/
twa/android-key.keystore
public/icons/*.bak

# Backups generados por scripts/backup.cjs (datos del negocio, no van al repo)
backups/
